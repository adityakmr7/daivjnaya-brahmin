import React from "react";
import { Box, Text } from "../../components";

interface b2bProps {}
const b2b = ({}: b2bProps) => {
  return (
    <Box flex={1}>
      <Text>b@b</Text>
    </Box>
  );
};

export default b2b;
