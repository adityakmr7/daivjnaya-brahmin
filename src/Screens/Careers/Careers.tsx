import React from "react";
import { Box, Text } from "../../components";

interface CareersProps {}
const Careers = ({}: CareersProps) => {
  return (
    <Box flex={1} justifyContent="center" alignItems="center">
      <Text>Careers</Text>
    </Box>
  );
};

export default Careers;
