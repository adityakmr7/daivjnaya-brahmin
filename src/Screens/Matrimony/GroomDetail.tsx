import React from "react";
import { Box, Text } from "../../components";

interface GroomDetailProps {}
const GroomDetail = ({}: GroomDetailProps) => {
  return (
    <Box>
      <Text>Groom Detail</Text>
    </Box>
  );
};

export default GroomDetail;
