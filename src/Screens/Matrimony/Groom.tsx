import React from "react";
import { Box, Text } from "../../components";

interface GroomProps {}
const Groom = ({}: GroomProps) => {
  return (
    <Box>
      <Text>Groom</Text>
    </Box>
  );
};

export default Groom;
